module Circle where
    -- area of a circle
    area :: Float -> Float
    area radius = radius * pi * radius

    -- perimetro de un circulo:
    perimeter :: Float -> Float
    perimeter radius = radius * 2 * pi